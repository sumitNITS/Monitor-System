version: 0.2

env:
  #variables:
     # key: "value"
     # key: "value"
  parameter-store:
    DOCKER_REGISTRY_URL: /dev/monitor-system/docker-registry/url
    DOCKER_REGISTRY_USERNAME: /dev/monitor-system/docker-credentials/username
    DOCKER_REGISTRY_PASSWORD: /dev/monitor-system/docker-credentials/password
  #secrets-manager:
     # key: secret-id:json-key:version-stage:version-id
     # key: secret-id:json-key:version-stage:version-id
  #exported-variables:
     # - variable
     # - variable
  #git-credential-helper: yes
#batch:
  #fast-fail: true
  #build-list:
  #build-matrix:
  #build-graph:
phases:
  install:
    runtime-versions:
      python: 3.12
      # name: version
    #commands:
      # - command
      # - command
  pre_build:
    commands:
      - echo "Installing the app requirements"
      # - pip3 install --no-cache-dir -r app_requirement.txt
      # - command
      # - command
  build:
    commands:
      - echo "Building the Docker Image"
      - docker build -t "$DOCKER_REGISTRY_URL/$DOCKER_REGISTRY_USERNAME/codebuild-flask-app:latest" .
      - echo "$DOCKER_REGISTRY_PASSWORD" | docker login -u "$DOCKER_REGISTRY_USERNAME" --password-stdin "$DOCKER_REGISTRY_URL"
      - docker push "$DOCKER_REGISTRY_URL/$DOCKER_REGISTRY_USERNAME/codebuild-flask-app:latest"
      # - command
      # - command
  post_build:
    commands:
      - echo "Build and Push the docker image to dockerhub completed!"
      # - command
      # - command
#reports:
  #report-name-or-arn:
    #files:
      # - location
      # - location
    #base-directory: location
    #discard-paths: yes
    #file-format: JunitXml | CucumberJson
artifacts:
  files:
    -'**/*'
    # - location
    # - location
  #name: $(date +%Y-%m-%d)
  #discard-paths: yes
  base-directory: .
#cache:
  #paths:
    # - paths